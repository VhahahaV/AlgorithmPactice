cmake_minimum_required(VERSION 3.24)
set(CMAKE_CXX_STANDARD 14)
PROJECT(AlgorithmPractice)
set(CODES ${CMAKE_SOURCE_DIR}/code)
file(GLOB DAYS ${CODES}/*)
foreach (day ${DAYS})
    if (IS_DIRECTORY ${day})
        message("当前变量是：${day} ")
        add_subdirectory(${day})
    endif ()
endforeach ()
